{
  "name": "hostmatch",
  "version": "0.1.0",
  "description": "Hostmatch is a command-line application to match individual webservers running locally with a sufficiently named hostname for easier development",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sudo node server.js"
  },
  "keywords": [
    "host",
    "vhost",
    "webserver"
  ],
  "author": "allwinpb@gmail.com",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "http-proxy": "^1.12.0"
  }
}
